version: "3.9"

services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: login-whitelabel
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
